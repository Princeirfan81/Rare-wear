const sb=supabase.createClient(window.RARE_SUPABASE_URL,window.RARE_SUPABASE_KEY);const grid=document.getElementById("productsGrid");async function load(){if(window.RARE_SUPABASE_KEY.includes("PASTE_")){grid.innerHTML="<p class='muted'>Connect Supabase in assets/config.js</p>";return}const {data,error}=await sb.from("products").select("*").eq("active",true).order("sort_order").order("created_at",{ascending:false});if(error){grid.innerHTML="<p class='muted'>Products could not be loaded.</p>";return}grid.className="products";grid.innerHTML=(data||[]).map(p=>`<article class="card">${p.image_url?`<img src="${p.image_url}" alt="">`:``}<div class="card-body"><h3>${e(p.name)}</h3><div class="price">${e(p.price)}</div><p class="muted">${e(p.description||"")}</p><a class="btn" href="https://wa.me/916362619930" target="_blank">Order</a></div></article>`).join("")||"<p class='muted'>No products yet.</p>"}function e(x){return String(x).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}load();